import { mount } from '@vue/test-utils'
import { NButton } from '../components/index'
import { describe, expect, test } from 'vitest'

describe('NButton', () => {
  // test('renders the correct style for primary', () => {
  //   const type = 'primary'
  //   const wrapper = mount(NButton, {
  //     props: { type },
  //   })
  //   expect(wrapper.classes()).toEqual(
  //     expect.arrayContaining(['n-button-primary'])
  //   )
  // })
  // test('renders the correct style for success', () => {
  //   const type = 'success'
  //   const wrapper = mount(NButton, {
  //     props: { type },
  //   })
  //   expect(wrapper.classes()).toEqual(
  //     expect.arrayContaining(['n-button-success'])
  //   )
  // })
  // test('renders the correct style for info', () => {
  //   const type = 'info'
  //   const wrapper = mount(NButton, {
  //     props: { type },
  //   })
  //   expect(wrapper.classes()).toEqual(expect.arrayContaining(['n-button-info']))
  // })
  // test('renders the correct style for warning', () => {
  //   const type = 'warning'
  //   const wrapper = mount(NButton, {
  //     props: { type },
  //   })
  //   expect(wrapper.classes()).toEqual(
  //     expect.arrayContaining(['n-button-warning'])
  //   )
  // })
  // test('renders the correct style for danger', () => {
  //   const type = 'danger'
  //   const wrapper = mount(NButton, {
  //     props: { type },
  //   })
  //   expect(wrapper.classes()).toEqual(
  //     expect.arrayContaining(['n-button-danger'])
  //   )
  // })
  // test('renders the correct style for disabled', () => {
  //   const disabled = true
  //   const wrapper = mount(NButton, {
  //     props: { disabled },
  //   })
  //   expect(wrapper.classes()).toEqual(
  //     expect.arrayContaining(['n-button-disabled'])
  //   )
  // })
  // test('renders the correct style for round', () => {
  //   const round = true
  //   const wrapper = mount(NButton, {
  //     props: { round },
  //   })
  //   expect(wrapper.classes()).toEqual(
  //     expect.arrayContaining(['n-button-round'])
  //   )
  // })
})
